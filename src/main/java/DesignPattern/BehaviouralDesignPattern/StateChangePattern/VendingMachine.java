package DesignPattern.BehaviouralDesignPattern.StateChangePattern;

/*
    * create interface of state and declare all possible action items on all the states
    * now create all different state classes and implement only required methods/ action items
    * change state at every step
 */
public class VendingMachine {
    public static void main(String[] args) {
        /*
            * implemented in LLD questions sections
         */
    }
}
